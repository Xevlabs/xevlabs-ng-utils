import { ParseEmptyFieldPipe } from './parse-empty-field.pipe';

describe('ParseEmptyFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseEmptyFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
