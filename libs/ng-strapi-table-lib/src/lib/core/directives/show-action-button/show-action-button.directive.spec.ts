import { ShowActionButtonDirective } from './show-action-button.directive';

describe('ShowActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowActionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
