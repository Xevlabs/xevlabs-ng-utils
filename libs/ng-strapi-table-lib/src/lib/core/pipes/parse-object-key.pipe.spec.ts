import { ParseObjectKeyPipe } from './parse-object-key.pipe';

describe('ParseObjectKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseObjectKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
