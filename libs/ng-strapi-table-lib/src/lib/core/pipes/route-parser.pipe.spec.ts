import { RouteParserPipe } from './route-parser.pipe';

describe('RouteParserPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteParserPipe();
    expect(pipe).toBeTruthy();
  });
});
